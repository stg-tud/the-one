[globals]
estimator = median
output_dir = ./output/
reports_dir = ../reports/

[graph]
context = paper
palette = muted
style = whitegrid

[graph.delivery]
quant = 300

[graph.delay]
quant = 300

[graph.stats]
stat = created, started, relayed, aborted, dropped, delivered, delivery_prob

[stats]
stat = created, started, relayed, aborted, dropped, delivered, delivery_prob
